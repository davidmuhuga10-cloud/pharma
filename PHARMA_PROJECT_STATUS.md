# Pharma — Project Status

**Last updated:** September 18, 2026 (later same-day update — "Sync common drugs" fast-onboarding feature shipped, a real live bug found and fixed (a duplicate `record_restock` overload was silently breaking every restock call — items 14 and 15), a branding/UI cleanup pass (item 16), a full app-wide "Command Green" redesign rolled out to every screen, desktop and mobile (item 17), real SMS-OTP verification added to password reset, closing roadmap A.1(b) (item 18), a live bug in the new OTP flow found and fixed the same day (item 19), a real chemist's name scrubbed from placeholder text (item 20), a full Suppliers module shipped — opening balances, LPOs that also restock inventory, supplier payments, and a per-supplier running-ledger statement (item 21), the Dashboard rebuilt with a real date-range filter, sales/rush-hours charts, four summary tiles, a Sales→Cost→Profit breakdown, capped Top Sellers/Expiring Soon lists with full-list views, fixed-size empty states for brand-new pharmacies, and an app-name rebrand from "Hodhi" to "Pharma" (item 22), a new Expenses module for general pharmacy costs — rent/utilities/salaries/etc., a by-category breakdown, and soft-voidable entries (item 23), and an SMS notify-on-password-reset tripwire closing the last open piece of roadmap A.1 (item 24). UI/UX is now a standing priority for this project, not a one-off — see section 2.

**Later same-day update (a 5-item user punch list):** Expenses' "Void" renamed to "Reverse" throughout, given a red background, more breathing room from the amount, and free-text custom categories instead of the fixed list (item 25); Excel import relaxed to require only drug name + quantity, with missing price defaulting to cost price (or 0) and missing expiry importing as "expiry unknown" (same placeholder-date pattern as Sync common drugs), flagged on Inventory with a "fix later" list that lets a pharmacist go back and fill in the real date per batch — a new `set_batch_expiry` RPC — (item 26); a Dashboard quick-access row added — a one-tap Sell shortcut plus a live "is this drug in stock?" search (item 27); a real bug fixed where the search boxes (Inventory, Sync common drugs, Sell, new LPO) lost focus on every keystroke, making it feel like typing was "blocked" after one character (item 28); and a desktop layout bug fixed where a CSS margin/width mismatch was leaving a large, unused empty margin on the right side of every screen (item 29). All 38 SQL regression tests still pass against the updated schema; two new manual checks confirmed the free-text expense category and the unknown-expiry batch flow end to end.

**Later same-day update (a 3-item punch list, in progress):** every plain-white summary tile in the app (drug detail sheet, Reports, Suppliers ledger, Expenses) and every plain-white Dashboard graph/list card (Sales trend, Rush hours, Stock health, Profit breakdown, Top sellers, Expiring soon) now carries a light tint from the same palette as the Dashboard's existing Sales/Stock value/Profit/Supplier-debt pills — color chosen by meaning (green = money in/healthy, orange = stock & goods, violet = a neutral count, red = money out/owed, amber = needs attention), not picked per screen. A color sketch was shown for approval before implementing (item 30). Every one of those same tinted tiles/pills/cards now also carries a colored left/right accent border — a thin 1px border all around plus a thicker 4px accent on the left and right edges, matching the tile's own hue — ported directly from the `.stat`/`.hub-tile` pattern in the ShuleTop codebase (`shule-web/src/styles/main.css`), which uses the identical `border-left/right:4px solid transparent` + `border-color:<hue>` technique (item 31). One item from the same punch list is still open: restructuring Reports into proper printable sub-modules (Sales Report, Expiry Dates Report, etc.) with a "Generated by Pharma" footer. Also flagged: while checking a user-reported layout gap, confirmed via the live site's own CSS that the deployed Netlify build still hasn't picked up this session's earlier fixes — the user needs to `git push` from their local `Pharma` folder to deploy everything done today.)

**Live state as of this update (checked directly against the Supabase project, not assumed from prior notes):** RAFIKI PHARMACY (owner "David Kinyua", phone `0705041512`, created 2026-09-17) had 132 drugs imported but 0 batches — at the time this looked like an incomplete two-step import, but **the real cause has since been found: item 15 below**. At the user's explicit request ("clear the records in that account, I want to do a fresh upload, it's my demo account") that pharmacy's drug rows were deleted this session (categories were kept) so they can re-import cleanly now that the underlying bug is fixed.
**Live site:** https://pharmacymngt.netlify.app
**This file's purpose:** Upload this document into a new Claude session (or paste it in) at any time and it will fully understand where this project stands — what's built, what's been fixed, what's still pending, and exactly how this project works day to day. This is the sister project to Shule Web/ShuleTop — same owner, same workflow rules, same accounts — see section 2 for what carries over and what's specific to this project.

---

## 1. What this is

**Pharma** (originally prototyped under the working name "Hodhi," Swahili for "hold/reserve/store" — renamed this session, item 22) is a pharmacy stock & sales management app built for Kenyan pharmacies. It's a multi-tenant, offline-capable PWA (web app installable to a phone home screen) covering: drug/batch inventory with FEFO (first-expiry-first-out) selling, point-of-sale with split payments (cash/M-Pesa/insurance), returns/voids, prescription capture, staff roles, insurance claims tracking, Excel import/export, and letterheaded printing.

It originated from a real pharmacy's (Rubao Mukothima's) paper stock-take spreadsheet — see `PRODUCT_SPEC.md` in this folder for the full origin story, Kenyan market context (eTIMS/KRA compliance, PPB inspection regime, competitor landscape), and the complete feature list as of "v2" (what shipped before this review).

**Backend:** Supabase (same pattern as Shule Web) — Postgres with Row Level Security, `pharmacy_id` scoping every table, SQL functions (`record_sale`, `record_restock`, `record_write_off`, `record_correction`, `record_return`, `void_sale`, `bootstrap_pharmacy`, `create_staff_invite`, `join_pharmacy_with_code`) doing the actual business logic server-side. See `schema.sql` for the complete schema, and `SETUP.md` for the original Supabase connection notes (the project was already live and smoke-tested before this review started).

---

## 2. Standing rules for this engagement — same as Shule Web/ShuleTop

This project follows the exact same working relationship and rules established on Shule Web. If you're a new Claude session picking this up, read `SHULETOP_PROJECT_STATUS.md` (if it's available to you) for the full detail — the short version:

- **"DON'T CHANGE IF UNSURE"** — the standing rule across both projects. Never widen a fix beyond what was asked; scope every change as narrowly as possible; ask rather than guess when a root cause is ambiguous.
- **File delivery workflow**: edit files in the Claude sandbox → verify (`node --check` per file, run in a real headless browser to catch runtime errors, screenshot to verify visual/UX claims) → deliver via `SendUserFile` → write into the user's real folder via the device-bridge `device_commit_files` tool (when connected) → give the user exact `git` commands to run themselves. **Claude never runs git commands on the user's behalf** — not because of a technical limitation this time (there's no sandboxed git repo to push from here at all; this is a fresh project), but because that's the standing rule on both projects.
- **Verification rigor**: don't ship a claim about how something looks or behaves without actually rendering it (headless Chromium via Playwright) and looking at the result. This was used throughout the initial code review (see section 4) to separate real bugs from testing artifacts.
- Every commit should carry the same attribution trailer used on Shule Web (check the active session's own instructions for the exact current text, since it can change).
- **UI/UX is a standing priority, not a one-off patch** (added this session, at the user's explicit direction): "treat UI/UX as a standing rule — think of it as our first sales agent — every page should be redesigned with this same level of polish and consistency, not just patched individually." In practice this means: (1) the app has a real design language now — "Command Green" (dark forest-green branded sidebar/bottom-nav with a gold active state, a slim white desktop topbar, bold color-coded KPI tiles, a segmented-capsule toolbar for auxiliary actions next to one clear primary action, avatar-initial list rows) — documented in item 17 below; (2) any new screen or feature added from here on should be built to match it, not styled ad hoc; (3) prefer fixing shared CSS classes (`.btn`, `.card`, `.kpi`, `.list-row`, `.toolbar-row`/`.toolbar-segment`, `.badge`, `.navbar`) over one-off inline styles, since this app's screens are almost entirely built from those shared classes and a well-designed shared-class change cascades to every page automatically; (4) when in doubt about a new design direction, sketch 2-3 genuinely different options as a Design canvas artifact for the user to pick from before implementing, the way item 17 was decided.

**What's different from Shule Web:**
- **Separate GitHub repo, separate Netlify site** — this was a deliberate choice (see section 5) so that deploying Pharma changes never touches or risks Shule Web, and vice versa. They share the same GitHub account (`davidmuhuga10-cloud`) and the same Netlify account, but are otherwise fully independent projects.
- **Separate local folder**: `C:\Users\user\Downloads\Pharma` on the user's computer (mirrors how Shule Web lives in `C:\Users\user\Downloads\shule-web`).
- **No version-number convention yet** — Shule Web uses "Version N" commit messages by long-standing convention. Pharma is new; adopt the same convention starting now if the user wants consistency (recommended), starting at "Version 1" for the initial import already committed.

---

## 3. How this project came to exist

The user uploaded a zip (`hodhi-pharmacy-app-v2_2.zip`) of an already-built app — built by a previous developer or AI session, not from scratch here — describing it as looking like "beginner" work with a bad login UI and asking for a senior-developer-style review, a roadmap, and then for all the issues found to be fixed. A full code review was done first (see section 4), which found the opposite of "beginner code" underneath: a well-architected multi-tenant backend with real security thinking, let down mainly by plain/generic visual design and a handful of fixable rough edges. That review is preserved as a Claude document (a "living doc," not a file in this repo) — ask the user for the link if you need the full original writeup; the essentials are captured in section 4 below regardless.

After the review, the user asked to set this up as its own project (this folder, this repo) so it can deploy independently via Netlify, then to work through the fixes identified.

---

## 4. Code review findings (from the initial audit)

### What's already solid — don't rebuild this
- Row Level Security on every table, correctly scoped by `pharmacy_id`
- Base `GRANT`s present for the `authenticated` role (the exact gap that silently breaks a lot of RLS-only setups — already handled here, see `schema.sql` near line 873)
- All dashboard views use `security_invoker = true` (prevents RLS bypass via view-owner privileges)
- A dedicated trigger (`prevent_profile_privilege_escalation`) stops a staff account from promoting itself to owner by editing its own profile row directly — a specific, non-obvious attack that a lot of hobby-grade apps miss entirely
- FEFO (first-expiry-first-out) batch selling, split payments, held/parked sales, sequential invoice numbers, prescription capture, item-level returns with double-credit protection on voids, staff invite codes (single-use, 7-day expiry, role-scoped)
- Bulk Excel import that understands the real messy `12.2027`-style expiry format from actual pharmacy stock-take sheets, real `.xlsx` export (not CSV), letterheaded print output everywhere
- Offline-first service worker (app shell only, never caches live stock data) with proper retry states instead of infinite spinners

### Real bugs/gaps found, and their status
| # | Issue | Status |
|---|---|---|
| 1 | Entire app depended on `cdn.jsdelivr.net` for the Supabase and Excel libraries, with no fallback — a slow/blocked CDN froze the app at "Loading Hodhi…" forever with no error | ✅ **Fixed** — both libraries vendored locally (`vendor-supabase.js`, `vendor-xlsx.js`), `index.html` updated to load them locally, plus a global error handler so a load failure now shows a real error + Reload button instead of an infinite spinner |
| 2 | No confirmation before destructive actions (write-off, void, correction) | ✅ **Fully fixed** — write-off and void already had a confirm gate; correction now has one too (`saveCorrection()` in `app.js`), closing item A.4 from the roadmap below |
| 3 | Password reset used a native browser `prompt()` popup — looks broken on mobile, blocked in some in-app browsers | ✅ **Fixed** — replaced with a proper `renderForgotPassword()` screen matching the rest of the auth flow |
| 4 | Whole app is one 1,706-line file with hand-built string concatenation and inline `onclick` handlers — fragile but not currently broken (verified escaping is correct everywhere it was checked) | 🔲 **Not started** — this is a refactor, not a bug; lower priority than the items below |
| 5 | No automated tests, especially for the SQL functions doing money/stock math (FEFO deduction, split-payment balancing, void/return credit) | ✅ **Fixed** — see `tests/` in this folder. A free, local, throwaway Postgres database (never the live Supabase project) loaded with the real unmodified `schema.sql` plus a minimal stand-in for Supabase's `auth` schema; `node run_tests.js` runs 18 checks covering bootstrap_pharmacy, FEFO batch draining order, split-payment validation, overselling rejection, void/return double-credit protection, staff invite single-use/role-scoping, and the privilege-escalation trigger. All 18 pass. See `tests/README.md` for how to run it again after a schema change |
| 6 | Swahili translation only covers navigation labels, not the whole app, despite the language toggle existing | ✅ **Resolved for now** — the language toggle is removed and the app is locked to English (`t()` in `app.js` always returns the English string, ignoring anything stored on the profile, and Settings shows a plain "English (Kiswahili is coming soon)" note instead of a dropdown). This avoids a pharmacist landing on a half-translated screen. Finishing the Swahili translation and bringing the toggle back is still a legitimate future item — the `STRINGS.sw` table in `app.js` is untouched and ready to be extended |
| 7 | Visual/UX: no logo or brand identity, desktop auth screen wastes most of the screen, single flat color palette, emoji used as icons, generic system font | ✅ **Fixed** — added a real logo mark (`logoMarkHtml()` in `app.js`, a green rounded badge, used on the topbar and every auth screen; originally a capsule glyph, later replaced with the real "H" mark — see item 16); replaced every emoji icon (nav bar, print/import/export/reorder buttons, cart, warnings, close buttons) with a hand-drawn inline SVG icon set (`ICONS`/`icon()` in `app.js` — no icon-font CDN dependency, same reasoning as vendoring Supabase/xlsx); redesigned the desktop auth screen as a real two-column split (brand panel with feature highlights + form), collapsing to a simple stacked layout on mobile; added subtle shadows/depth to cards, buttons and the sheet modal instead of flat borders only. Verified in a real browser (screenshots taken) before shipping. Font is still the system stack (no new external font dependency added, deliberately, to avoid reintroducing the CDN-reliability problem from bug #1) |
| 8 | Backups | ❓ **Unconfirmed** — need to check that Supabase point-in-time recovery is switched on for this project (a one-click Supabase dashboard setting, not a code change) |
| 9 | eTIMS (KRA e-invoicing) integration | 🔲 **Not started** — not urgent today, but flagged as a real compliance deadline coming for VAT-registered Kenyan pharmacies; the sales record already has the fields designed to support it later without a rebuild |
| 10 | Login required an email — pharmacy staff reliably have a phone number, not necessarily a checked email | ✅ **Fixed** — login, signup and staff-join screens now ask for a **phone number**, not email. Supabase Auth is still email-shaped under the hood, so `app.js` derives a stable, never-shown "auth email" from the phone number (`phoneToAuthEmail()` in `app.js`, e.g. `0712345678` → `p254712345678@hodhi.local`) and uses that everywhere `sb.auth.*` needs an email. **Action needed in the Supabase dashboard**: turn OFF "Confirm email" under Authentication → Providers → Email — the derived address can't receive a confirmation mail, so with confirmation on, every new signup would be locked out with no way to confirm. The one pre-existing account (`Rubao Mukothima Pharmacy`, owner "Daudi", a demo/test account) was deleted from the live Supabase project at the user's request — the project now has 0 pharmacies, 0 profiles, 0 auth users, so the first real signup will be a clean start |
| 11 | Forgot password | ✅ **Fixed, still deliberately temporary/insecure past the rate limit — see the security note in section 6.** The Forgot Password screen asks only for the account's registered phone number and a new password, and changes it immediately — no OTP, no code, no confirmation. Implemented as a Supabase Edge Function, `reset-password-by-phone` (`verify_jwt` off), because changing another user's password needs the service-role key, which must never reach the browser. **Update (this session):** now rate-limited server-side — max 3 attempts per phone number per rolling hour, backed by a new `password_reset_attempts` table (RLS enabled, zero policies, so only the Edge Function's service-role key can read/write it — `authenticated`'s blanket table grant resolves to no access). Deployed as version 2 and live. This closes roadmap item A.1(a) (the cheap win); a real SMS OTP (A.1(b)) is still the actual fix and still open — anyone who knows a phone number can still eventually get 3 tries an hour at guessing/using it |
| 13 | **New finding, this session**: `record_restock`, `record_correction`, `record_write_off`, `void_sale`, and `record_return` only checked "does the caller belong to this pharmacy" — not their role. `app.js`'s own `PHARMACIST_ACTIONS`/`ATTENDANT_ACTIONS` gate already assumed these five are pharmacist/owner-only in the UI, but nothing stopped an authenticated attendant from calling the RPCs directly (browser console, curl, their own valid session) and bypassing that UI-only gate | ✅ **Fixed.** All five now also check `my_role() in ('owner','pharmacist')`, matching `app.js`'s existing allow-list exactly. Verified against a local throwaway Postgres loaded with the real schema: attendant blocked on all five (12/12 checks), owner/pharmacist unaffected, `record_sale` (intentionally open to every role) untouched. Applied directly to the live project (migration `enforce_role_on_restock_correction_writeoff_void_return`); `schema.sql` and `tests/run_tests.js` updated to match |
| 12 | The bulk Excel importer (built specifically for real stock-take sheets, see section 4's "what's already solid") actually failed on a real one | ✅ **Fixed.** Tested directly against a real stock-take file the user uploaded (`RUBAO MUKOTHIMA STOCK TAKE`, 312 rows) and found the importer brought in **0 of 312 rows** — the sheet has a title row above the real column headers ("DRUGS / QTY / PRICE PER UNIT / EXP DT" is row 2, not row 1), which broke the assumption that row 1 is the header row, so every value landed under the wrong key. Fixed in `app.js`: the importer now reads the file as raw rows and searches the first several rows for the one that actually contains a recognized column name, instead of assuming it's row 1; it also now recognizes the header "DRUGS" (plural, as real sheets use it), and treats an ALL-CAPS row with a name but no quantity/price (e.g. "ANALGESICS/ANTIPYRETICS", "COUGH SYRUPS") as a category heading, carrying it forward onto the drug rows beneath it — matching how a person reads the sheet by eye. Category names are now also matched against existing categories ignoring punctuation/spacing (so "ANTACIDS/ANTI H PYLORI" matches the seeded "Antacids/Anti-H-Pylori"). Re-tested against the same real file: **131 rows now import correctly**, 157 are correctly skipped (no current quantity — they're catalog items this stock-take shows as out of stock, not a bug), 19 rows are recognized as category headers rather than stock lines, and the rest are blank spacer rows in the sheet. See section 6 for what this does and doesn't mean for "can Hodhi replace the manual spreadsheet" |
| 14 | **New feature, this session**: fast pharmacy onboarding — a shared "master drug catalog" a new pharmacy can browse/search and tick off instead of typing every drug by hand | ✅ **Shipped.** New `master_drugs` table (287 items across the same 16 categories `bootstrap_pharmacy` seeds, curated from the real `RUBAO MUKOTHIMA STOCK TAKE` sheet — the same source this whole project started from), read-only for every tenant (RLS enabled, SELECT-only policy, same zero-write pattern as `password_reset_attempts`). New RPC `sync_master_drugs` (owner/pharmacist only, same tier as restock): for each selected item, reuses an existing `drugs` row by name if the pharmacy already has one (never overwrites its reorder level, only refreshes price, exactly like a restock) or creates one from the catalog's name/form/unit/category, then creates a batch + `stock_adjustments` row. Expiry date is optional — omit it and the batch gets a placeholder date 3 years out flagged `expiry_unknown` (a new `batches` column), so it's excluded from FEFO/"expiring soon" until a real date is known; the UI shows "Expiry unknown" instead of that placeholder date. New "Sync common drugs" button in the Inventory tab, alongside "+ Add new drug" and "Import" — a third, additional way to stock a pharmacy, not a replacement for either. Verified locally (11 new checks in `tests/run_tests.js`, all passing: role enforcement, no-duplicate-drug-on-resync, reorder-level-never-clobbered, placeholder-expiry behavior, validation) and in a real headless-browser run of the actual UI (search, select, inline fields, submit, exact RPC payload) before shipping. Live migration `add_master_drugs_catalog_and_sync_rpc` applied. The form/unit values are a best-effort default from each item's name (mostly right, not clinically verified) — cosmetic only, freely editable by editing the drug directly once it exists; worth a quick skim of `master_drugs` in Supabase if you want to sanity-check the categorization yourself |
| 15 | **New finding, this session — a real live bug, not just a gap**: two overloads of `record_restock` existed live (the original, and a second one added earlier this session to support an optional/unknown expiry, as a *separate* function instead of extending the first). This made **every normal restock call from the app ambiguous** — Supabase/PostgREST calls RPCs with named parameters, and Postgres couldn't choose a unique candidate between them, raising `function record_restock(...) is not unique` and refusing the call. Reproduced directly (see item 12's neighbor above and the live-state note at the top of this file) — **this is almost certainly the actual cause of RAFIKI PHARMACY's stalled import**: 132 drugs went in fine (a direct insert, no RPC involved), but every restock call behind those drugs silently failed this way, and the importer's per-row error handling just counts a failure without surfacing the real Postgres error. The second overload was also missing the owner/pharmacist role check item 13 added to the first | ✅ **Fixed.** Collapsed back to a single `record_restock` function (`p_expiry_unknown` as a defaulted 10th argument, not a second overload), role check restored. Verified live: only one function signature exists now, and the exact named-parameter call shape that failed before now succeeds. `schema.sql` and `tests/run_tests.js` updated (a new regression-guard test asserts `record_restock` never has more than one overload again) |
| 16 | **New request, this session**: the loading screen said the generic "Loading Hodhi…" with no logo and no centering; the brand mark (`logoMarkHtml()`) was still the old capsule/pill icon glyph from item 7, not the actual "H" mark the app's own PWA icons (`icons/icon-*.png`, referenced in `manifest.json`) use; the app and login backgrounds were both plain near-white (`--paper: #FAF9F7`); and the auth screen had some redundant lead-in copy ("New pharmacy? Create an account", "Joining a pharmacy? Use a staff code") | ✅ **Fixed.** `logoMarkHtml()` (`app.js`) now renders a bold "H" in the existing green badge instead of the capsule icon — matches the real app icon, used consistently on the topbar, every auth screen, and the new loading screen. `index.html`'s initial `#app` content is now the same centered `.auth-wrap-simple` card layout used elsewhere in the app, showing the H badge and "Loading your dashboard…" instead of raw unstyled text. `--paper` (the page background variable, `style.css`) changed from `#FAF9F7` (near-white) to `#EFE8D8` (soft warm muted cream) — this is the shared background for the whole app *and* the login/signup/join screens, since the auth form column already sat on the body background rather than its own; `manifest.json`'s `background_color` updated to match, so the PWA splash screen is consistent too. Auth-screen toggle copy trimmed: login now reads "Create an account · Staff Login" (was "New pharmacy? Create an account · Joining a pharmacy? Use a staff code"), signup reads "Log in · Staff Login" (was "Already have an account? Log in · Joining a pharmacy? Use a staff code") — "New Pharmacy" wording is gone entirely and "Staff Code" is now "Staff Login" everywhere it appeared as a section/link label; the join screen's actual "Staff code" input field label is unchanged since that's the literal field you paste the code into, not a section label. Verified with `node --check` and real headless-Chromium screenshots of the loading screen, login/signup/join screens (desktop split-panel and mobile stacked layouts) and the logged-in topbar before shipping |
| 17 | **New request, this session**: the user flagged the desktop Stock screen specifically (uneven-height toolbar buttons that wrapped awkwardly — "Sync common drugs" ran to two lines and stood taller than its neighbors — a plain white sidebar/topbar, and a full-width flat "Reorder list" bar) as "not the best UI," and asked for 3 creative full redesign directions to choose from, then to roll the chosen one out consistently across every page and screen size, and for UI/UX to be treated from now on as a standing priority (see item 2's new standing-rules bullet), not something patched screen-by-screen | ✅ **Shipped.** Sketched 3 genuinely different desktop directions as a Design-type Artifact canvas (an editable mockup page, not committed code) — "Quiet Clinic" (airy/editorial), "Command Green" (bold branded, dark-green sidebar + gold accents), "Dispense Grid" (dense data-table utility) — the user picked **Command Green**. Implemented app-wide by restyling the shared CSS component classes every screen already builds from (`.navbar`, `.topbar`, `.btn`, `.toolbar-row`, the new `.toolbar-segment`, `.kpi`/`.kpi-grid`, `.card`, `.list-row`, the new `.row-avatar`, `.badge`) in `style.css`, plus two small `app.js` changes: `navBar()` now also renders a brand block (H badge + "Hodhi" wordmark) and an account-identity footer card into the same `.navbar` element, both hidden on mobile and shown only in the desktop sidebar via CSS; `drawInventory()`'s toolbar is now a bordered `.toolbar-segment` capsule (Sync/Import/Excel/Print) with one clear primary "+ Add new drug" CTA beside it, its list rows now lead with a colour-coded initial avatar, and the flat "Reorder list" bar is now a quiet inline amber notice showing the real reorder count instead of a static label. Desktop (≥860px): the bottom tab bar becomes a **full-height** dark-green sidebar (brand at top, gold-pill active state, pharmacy/user identity card at the bottom) and the old full-width green topbar becomes a slim white utility strip beside it (pharmacy name + Log out) — this is a genuine layout restructure (the sidebar used to start below the topbar; now it runs the full viewport height and the topbar is offset beside it), not just a colour change. Mobile (<860px): the bottom tab bar keeps its usual position but now carries the same dark-green-and-gold language instead of plain white-with-green-text. Because Home/Stock/Sell/Reports/Claims/Settings are all built from the same shared classes, this one CSS/JS pass visibly upgraded every one of them, not just the Stock screen the complaint was about — also applied the `.toolbar-segment` treatment to the Reports and Reorder-list Excel/Print button pairs for the same reason. Verified with `node --check` and a real headless-Chromium run rendering all 5 tabs at both a 1440px desktop width and a 390px mobile width against mocked data (no live Supabase calls made) before shipping |
| 18 | **New request, this session**: "improve the security of the app... benchmark how Shule Web does every login/OTP, implement OTP as well" — closing roadmap item A.1(b) (item 11: "a real SMS OTP is still the actual fix" for password reset, which until now let anyone who knew a phone number set a new password immediately, no code) | ✅ **Shipped.** Benchmarked Shule Web's real phone-OTP architecture first (its `send-otp.js`/`verify-otp.js`/`_lib/otp.js`/`_lib/smsProvider.js`/`forgot-password.js` and `phone_otps` migration, all read in full), then ported the same design to Pharma's own Supabase project (id `ovegzdlrhlcpjfcsoumu`): new `phone_otps` table (hashed 6-digit codes, salted by phone+purpose so no plaintext code is ever stored; 5-minute expiry; max 5 verify attempts, burned once exhausted; RLS enabled, zero policies — server-only, same pattern as `password_reset_attempts`) and `sms_platform_config` table (single row, same zero-policy pattern, seeded with **Shule Web's own Africa's Talking credentials** — username `kodirentals`, sender ID `Flovo_sms` — reused at the user's explicit direction rather than standing up a separate Pharma-specific SMS account; both apps' OTP texts now bill against the same AT wallet and go out under the same sender name, which the user was told and accepted). Two new public Edge Functions deployed: `send-otp` (rate-limited per phone+purpose — 30s resend cooldown, max 5 sends/15min, enforced directly against `phone_otps`) and `verify-otp` (checks the code, on success issues a short-lived HMAC-signed `verified_token` scoped to that exact phone + `'password_reset'` purpose, 15-minute validity, `SUPABASE_SERVICE_ROLE_KEY` reused as the HMAC secret rather than adding a new one). `reset-password-by-phone` (the existing Edge Function from item 11) redeployed as version 3: now **requires** a valid `otp_verified_token` for the submitted phone before it will touch the account — knowing the phone number alone is no longer enough. The original 3-per-hour rate limit stays as defense in depth. `app.js`'s forgot-password screen is now a real 3-step flow (phone → SMS code → new password) instead of one screen — `renderForgotPassword()`/`drawForgotPassword()`/`doSendResetCode()`/`doVerifyResetCode()`/`doSetResetPassword()`, including a "Resend code" link and per-step error handling matching each function's actual response shape. Deliberately scoped to `password_reset` only for now, not signup — the user confirmed this explicitly; the same `phone_otps` infrastructure can be extended to signup later by widening one check constraint if wanted. Verified: 17 standalone crypto/token-logic checks (hash determinism and salting, token sign→verify round-trip, wrong purpose/phone/secret all correctly rejected, tampered/malformed/expired tokens all correctly rejected — this sandbox's network egress can't reach `*.supabase.co` directly to invoke the live functions, so the exact same Web Crypto algorithm was re-run standalone against a Node harness to confirm it round-trips correctly before trusting the deployed Deno code); `node --check app.js`; and a real headless-Chromium run of the actual new 3-step UI against a mocked `sb.functions.invoke` driving every branch — empty/short-phone validation, a rate-limited response, successful send, wrong-code error with attempts-remaining message, resend, correct code, short-password validation, and a full successful reset back to the login screen, with the exact call payloads captured and confirmed to match what the deployed functions expect. **Still worth doing, not done this session**: no real end-to-end SMS was sent/received (this sandbox has no path to trigger a real Africa's Talking send or receive a real text) — the first live password reset on the actual site is the real end-to-end confirmation and is worth doing once this is pushed |
| 19 | **Live bug, found by the user within minutes of item 18 shipping**: the very first real test of "Send code" on the live site failed with the generic error "Edge Function returned a non-2xx status code" instead of anything useful | ✅ **Fixed.** Root cause: `send-otp`, `verify-otp`, and `reset-password-by-phone` (inherited from before item 18, never actually hit until now) all returned HTTP 400/404/429/500 for expected outcomes (bad phone, rate limited, wrong code, etc.), with the real message in the JSON body — a pattern copied faithfully from Shule Web's own `send-otp.js`/`verify-otp.js`/`forgot-password.js`. The difference: Shule Web's frontend calls those with plain `fetch()`, which always lets you read the body regardless of status. Pharma's `app.js` calls Edge Functions through `sb.functions.invoke()` (the Supabase JS client), which silently discards the response body on any non-2xx status — `data` comes back `null` and `error.message` is a fixed generic string, no matter what the function actually said. Every specific message this session wrote (rate-limit countdowns, "incorrect code, N attempts left", "no account found", etc.) was unreachable the moment it needed a non-2xx status to signal a real error. Fixed by redeploying all three functions so every expected outcome returns HTTP 200 with the result in the body (`ok:false`/`error:` + a message) — a non-2xx is now reserved only for requests this app's own client never sends (wrong method, unparseable JSON). Deployed live: `send-otp` v2, `verify-otp` v2, `reset-password-by-phone` v4. No `app.js` change was needed — its error handling (`data.message`/`data.error`) was already correct; the bug was entirely in the functions' status codes. Not yet reverified against a real live click (redeployed and reasoned through; the user was asked to retry) |
| 20 | **New request, this session**: the Settings → pharmacy address field's placeholder text read "e.g. Rubao Market, Tharaka-Nithi County" — a real chemist's name, not a made-up example | ✅ **Fixed.** Changed to "e.g. Medicare Pharmacy" in `app.js`. Checked the rest of the project for the same string first — the only other "Tharaka-Nithi" mention is in `PRODUCT_SPEC.md`'s competitive-landscape section ("Nobody has a lock on Tharaka-Nithi or the smaller upcountry chemists"), which is a market-region reference in an internal planning doc, not the same placeholder and not user-facing — left untouched since it wasn't what was flagged |
| 21 | **New feature request, this session**: Settings → "Suppliers" had no real management — no opening balances, no LPOs (Local Purchase Orders) on delivery, no way to record a payment against a supplier's balance, no per-supplier statement. Remove "Add Supplier" from Settings entirely; it should live inside a proper Suppliers module | ✅ **Shipped.** Sketched 3 directions on a Design-canvas artifact first (A — Running Ledger, B — Balance Dashboard, C — LPO Documents); the owner picked **A, Running Ledger**, with one wording fix ("delivery" → "Delivery" in ledger entry labels), and chose **integrated** LPOs — creating an LPO also restocks inventory in the same call, rather than a separate financial-only record, so a delivery is entered once. New Supabase migration (`add_suppliers_module`): `pharmacies.next_lpo_no` (same sequential-numbering pattern as `next_invoice_no` → `LPO-000001`, …), `suppliers.opening_balance`/`opening_balance_date`, and three new tables — `supplier_lpos` (one row per delivery), `supplier_lpo_items` (line items, each linked to the `batches` row it created), `supplier_payments` — all RLS-enabled with the same `pharmacy_id = my_pharmacy_id()` tenant policy as every other table. Two new `security definer` RPCs matching `record_restock`'s authorization pattern (owner/pharmacist only): `record_supplier_lpo(p_pharmacy_id, p_supplier_id, p_items, p_delivered_at, p_notes)` — atomically creates the LPO header, one `batches` row and one `supplier_lpo_items` row per line item (reusing `record_restock`'s exact insert logic, including the `expiry_unknown` → 3-years-out placeholder handling), and totals the LPO — and `record_supplier_payment(p_pharmacy_id, p_supplier_id, p_amount, p_method, p_reference, p_paid_at, p_notes)`. New `app.js` "Suppliers" tab (nav item with a new truck icon, gated by the existing `can('suppliers')` permission — owner/pharmacist only, same as before): a supplier list with a colored owed/credit/settled badge per supplier; a per-supplier detail screen with a KPI strip (Opening balance / Total delivered / Total paid / Balance owed), a toolbar ("+ New LPO", "Record payment", Excel export, Print statement), and a single chronological running-ledger statement (opening balance → every delivery, labelled "LPO-000001 · **Delivery**" per the wording fix → every payment → running balance recalculated client-side after each entry); a "New LPO" multi-drug entry form modeled directly on the existing `openSyncMasterDrugs` tick-to-select pattern (STATE-keyed-by-drug-id, per-item fields written straight to STATE with no re-render so typing in one row never disturbs another) — quantity/cost price/sell price/batch no./expiry (or "expiry unknown") per drug, one `record_supplier_lpo` call submits the whole delivery; a "Record payment" form (amount/method/reference/date/notes). `openAddSupplier()`/`saveSupplier()` adapted in place (added opening-balance + as-of-date fields, now refreshes the Suppliers list instead of Settings) and the old Settings → Suppliers section removed outright, exactly as asked. Verified: `node --check app.js`; and a real headless-Chromium run against a mocked Supabase client seeded with a supplier carrying an opening balance, an existing delivery and an existing payment — confirmed the list's owed/settled badges, the detail screen's KPI math and running balance, the "· Delivery" capitalization, adding a new delivery (multi-item form → correct `record_supplier_lpo` payload → ledger and KPIs update correctly afterward), recording a payment (ledger/balance update correctly), the exact validation message when a selected drug is left without a quantity (submission correctly blocked, sheet stays open, no RPC fired), adding a new supplier with an opening balance, and — regression check — that Settings no longer shows a "Suppliers" heading or "+ Add supplier" button anywhere, with zero console/page errors throughout |

---

## 5. Deployment setup — journey and current state

**Decision made:** Pharma gets its own GitHub repo and its own Netlify site, fully separate from Shule Web, even though both share the same GitHub account (`davidmuhuga10-cloud`) and Netlify account. Reason: keeps deploys independent — a Pharma push can never accidentally affect the live Shule Web site or vice versa.

**Steps completed:**
1. Created `C:\Users\user\Downloads\Pharma` on the user's computer and copied in the full app (all files from the original zip, plus the Phase 1 fixes above) via the Claude device bridge.
2. Ran `git init`, `git add -A`, `git commit -m "Version 1: initial import"`, `git branch -M main` locally in that folder — succeeded, 20 files committed.
3. **Hit a snag**: the first push failed because the user was signed into a *different* personal GitHub account (`David-kinyua`) than the one Shule Web uses (`davidmuhuga10-cloud`), and had created the `pharma` repo there by mistake. Corrected by deleting that repo and creating a fresh empty `pharma` repo under `davidmuhuga10-cloud` instead, then re-pointing the git remote (`git remote set-url origin https://github.com/davidmuhuga10-cloud/pharma.git`) before pushing again.
4. **Next**: confirm the push to `davidmuhuga10-cloud/pharma` succeeded, then connect that repo to Netlify (Netlify dashboard → Add new site → Import an existing project → GitHub → select `pharma`; no build command needed, it's a static site like Shule Web; publish directory `/`).

**If you're picking this up and don't know whether the push/Netlify connection succeeded:** ask the user, or check https://github.com/davidmuhuga10-cloud/pharma directly to see if the code is there, and check the user's Netlify dashboard for a `pharma`-named site.

---

## 6. Roadmap — what's done, and what's left to reach Shule Web-level reliability

Shule Web earned its reliability over many small rounds: real bugs fixed as they were found, visual polish, and enough day-to-day battle-testing that "it just works" became true rather than assumed. Pharma is earlier in that curve — the backend architecture was solid from day one (see section 4's "what's already solid"), and this engagement has now closed most of the gaps between "well-built" and "reliable." What's below is the honest remaining distance, grouped by what kind of risk each item is, in priority order within each group.

### Done so far (for quick reference — full detail in section 4)
- ✅ Removed the hard CDN dependency (Supabase/Excel libraries vendored locally) — item 1
- ✅ Confirmation before write-off — item 2 (partial; correction still has none)
- ✅ Real forgot-password screen instead of a native `prompt()` — item 3 (superseded by item 11)
- ✅ 18 automated SQL tests covering FEFO, split payments, void/return, invites, privilege escalation — item 5
- ✅ Full visual/UX redesign — logo, icon set, two-column auth layout, shadows/depth — item 7
- ✅ Phone-number login end to end — item 10
- ✅ Immediate, code-free password reset by phone number — item 11
- ✅ Language locked to English, Swahili toggle removed until it's fully translated — item 6
- ✅ Bulk Excel import fixed and verified against a real stock-take sheet — item 12
- ✅ Live, working deploy at https://pharmacymngt.netlify.app, on its own repo/Netlify site
- ✅ Demo/test data cleared from the live Supabase project
- ✅ RPC-level role enforcement on restock/correction/write-off/void/return, matching the UI's own gate — item 13
- ✅ "Sync common drugs" fast-onboarding feature (287-item shared catalog, tick-to-add) — item 14
- ✅ Fixed a real live bug where restocking was silently broken by an ambiguous duplicate function — item 15
- ✅ Real "H" logo everywhere (topbar, auth screens, loading screen), softer muted background app-wide and on login, simplified auth-screen copy — item 16
- ✅ Full "Command Green" redesign rolled out app-wide (full-height branded sidebar, slim desktop topbar, segmented toolbars, bold KPI tiles, avatar-initial rows), desktop and mobile — item 17
- ✅ Real SMS-OTP verification added to password reset (phone → code → new password), closing roadmap A.1(b) — item 18
- ✅ Full Suppliers module — opening balances, LPOs that also restock inventory in one step, supplier payments, and a per-supplier running-ledger statement — item 21
- ✅ Dashboard rebuilt with a real Today/Week/Month/Year filter, a sales trend chart, a new "Rush hours" chart (3-hour buckets, backed by the existing `sales.sold_at` timestamp — no schema change needed), four summary tiles (Sales / Stock value / Profit / Supplier debt owed), a Sales→Cost→Profit breakdown using actual per-batch cost (not a current-price guess), and Top Sellers / Expiring Soon lists capped to 4 rows with a "View all" full-list sheet for pharmacies with long lists; every chart/tile/list renders a fixed-size, sensible placeholder before any data exists so a brand-new pharmacy's dashboard never looks broken or jumps around as data starts coming in. App display name changed from "Hodhi" to "Pharma" everywhere the user sees it (topbar, auth screens, print footer, Excel export filenames, PWA app name) — the "H" logo mark itself (badge letter and app icons) was deliberately kept unchanged, and a few internal-only identifiers with real consequences if renamed (the `@hodhi.local` synthetic auth email domain existing users are already logged in with, and the `hodhi_pending_signup` localStorage key) were also deliberately left as-is — item 22
- ✅ New Expenses module — a general pharmacy-expenses ledger (rent, utilities, salaries, transport, licenses, marketing, maintenance, supplies, other), separate from Suppliers (which is specifically drug-stock deliveries and supplier debt). Same Today/Week/Month/Year range filter as the dashboard, a total-spent + entry-count summary, a by-category breakdown, and a chronological list with a soft-void action (same audit-trail pattern as void_sale — a voided expense stays visible but drops out of totals). Owner/pharmacist only, same tier as Suppliers; not visible to attendants. Does **not** yet feed into the Dashboard's Profit tile (Profit there is still sales minus cost of goods sold only) — deliberately left alone this session rather than silently redefining what "Profit" means on an existing screen — item 23
- ✅ Closed the last open piece of roadmap A.1 — `reset-password-by-phone` now sends a best-effort SMS notice to the account's own phone right after a successful reset, so a takeover attempt doesn't go unnoticed even with OTP already in place — item 24
- ✅ Expenses: "Void" renamed to "Reverse" (button text, function names, confirmation copy), given a red background instead of gray, more gap from the amount, and free-text custom categories (a `check` constraint, not the old fixed enum, so one pharmacy's custom category never leaks into another's dropdown) — item 25
- ✅ Excel import relaxed to genuinely match a messy real-world stock-take sheet — only drug name and quantity are required now; a missing sell price defaults to cost price (or a visibly-flaggable KES 0), and a missing expiry imports as "expiry unknown" (the same placeholder-date pattern already used by Sync common drugs/supplier LPOs) instead of being skipped. Inventory now shows a "drugs have a batch with no expiry date set" banner linking to a new fix-it list where a pharmacist looks up the exact batch and sets the real date once they have it (new `set_batch_expiry` RPC) — item 26
- ✅ Dashboard quick access — a one-tap "Sell" shortcut button plus a live search box that shows whether a specific drug is in stock (with an in-stock/low/out badge) without leaving the dashboard, each result also offering a direct jump into Sell or Inventory — item 27
- ✅ Fixed a real bug where every live-search box (Inventory, Sync common drugs, Sell, new supplier LPO) lost keyboard focus on each redraw, making it feel like the search field "blocked" further typing after one character until clicking back in — item 28
- ✅ Fixed a desktop-only layout bug leaving a large, unused margin on the right side of every screen — an explicit `margin-left` (to clear the fixed sidebar) combined with an inherited `margin-right:auto` was pushing all leftover horizontal space into that margin instead of letting the content use it; fixed with an explicit `calc()` width — item 29
- ✅ Tinted dashboard-style backgrounds rolled out everywhere — every summary tile (drug detail, Reports, Suppliers ledger, Expenses) and every Dashboard graph/list card (Sales trend, Rush hours, Stock health, Profit, Top sellers, Expiring soon) now carries a light tint from the same palette already used on the Dashboard's Sales/Stock value/Profit/Supplier-debt pills, chosen by meaning rather than per-screen — shown as a color sketch for approval first, then implemented — item 30
- ✅ Colored left/right accent borders added to those same tiles/pills/cards — a 1px border all around plus a thicker 4px accent on the left/right edges in the tile's own hue, the exact technique used by ShuleTop's `.stat`/`.hub-tile` tiles (found in `shule-web/src/styles/main.css` and ported over as-is) — item 31

### Can the manual stock-take spreadsheet be retired now?
**Getting close, but not yet, and not because of the app.** The importer itself now genuinely works — tested against your real `RUBAO MUKOTHIMA STOCK TAKE` sheet, it correctly pulled in 131 real stock lines with the right drug names, quantities, prices, expiry dates, and (mostly) categories, with no manual retyping. That's the part that was actually broken before and is now fixed and proven, not just assumed.

What still stands between "the importer works" and "stop keeping the spreadsheet":
- **This was a one-time import test, not the live workflow.** The 131 rows haven't been imported into your real Supabase project yet — that's a deliberate choice (a live import is a real, hard-to-undo change to your actual data) and needs your go-ahead first, ideally after you've turned off "Confirm email" and can log in as yourself to do it, or by having me do it if you'd rather.
- **A handful of category names won't auto-match** — two of the sheet's section headers ("ANTIBIOTICS/ANTIFUNGAL/AMOEBICIDES/AZOLES" and a typo'd "POWDEERS/CREAMS") don't line up with the app's seeded category list, so those specific drugs will import without a category and need a quick manual re-categorize afterward. Not a blocker, just a known small cleanup.
- **The spreadsheet is a point-in-time stock take, not day-to-day stock keeping.** Even after import, going forward you'd need to actually use the app for restocks/sales/write-offs day to day (not just import once) for it to stay accurate — that's a habit change for whoever runs the counter, not a code question.
- **Ongoing categorization and new-drug entry** for anything not already in the sheet still happens by hand in the app (or a future import), same as it would in a spreadsheet.

Bottom line: the tool is ready to do the heavy lifting. Whether the manual spreadsheet can actually stop being used depends on doing one real import and then committing to using the app day-to-day — both are next steps for you, not open code work.

### A. Security hardening — do before onboarding any pharmacy you don't personally know
1. **Tighten `reset-password-by-phone` (item 11).** ✅ (a) rate limit done earlier this session — max 3/phone/hour, server-side, live. ✅ (b) **done** (item 18) — a real SMS OTP is now required before a password can be reset: `send-otp`/`verify-otp` Edge Functions, `phone_otps` table, and a short-lived signed `verified_token` that `reset-password-by-phone` requires. ✅ (c) **done this session (item 24)** — `reset-password-by-phone` (now v6) sends a best-effort SMS notice ("Your Pharma account password was just changed. If this wasn't you, contact your pharmacy owner immediately.") to the same phone the password was just changed on, right after a successful reset — the standard account-takeover tripwire, same idea as a bank/email "we noticed a change" notice. Fire-and-forget: a delivery failure (or the SMS provider being unreachable) never blocks or fails the reset itself, since the reset already succeeded before it's sent. All three parts of item 11/A.1 are now closed.
2. **Turn off "Confirm email"** in the Supabase dashboard (Authentication → Providers → Email). ✅ **Confirmed already off** — checked `auth.users` directly: the live signup's `email_confirmed_at` is set within 2 seconds of `created_at`, so this either was already off or is being auto-confirmed; either way phone signups are completing fine. No action needed.
3. **Confirm point-in-time backups are enabled** (item 8) — Supabase dashboard, Database → Backups. **Still not checkable/settable from here** — no MCP tool exposes project-level backup config (only database-level tools like migrations/SQL are available). Needs a one-click dashboard visit from you: Project → Database → Backups → confirm "Point in Time Recovery" (or at minimum daily backups) is on.
4. **Revisit `record_correction`'s lack of confirmation** (item 2). ✅ Done.
5. **Enable "Leaked password protection"** (HaveIBeenPwned check) — flagged by Supabase's own security advisor. **Still dashboard-only** — Authentication → Policies (or Authentication → Providers → Email, depending on dashboard version) → toggle "Leaked password protection" on. No MCP tool exposes Auth provider config, only database-level operations, so this is the one remaining manual step from the whole A.1–A.6 list.
6. Role enforcement gap on restock/correction/write-off/void/return (item 13) — RPC-level authorization now matches the UI's own role gate. ✅ Done.

**A.1–A.6 status: 4 of 6 fully closed from code, 2 are one-click Supabase-dashboard settings (A.3 backups, A.5 leaked-password protection) that no available tool can set — flagged for you above with the exact menu path.**

### B. Reliability & data integrity — the "does it just work" category
5. **Extend the automated tests past SQL.** The 18 tests in `tests/` cover the money/stock-critical database functions, which is where a silent bug would be most expensive — but nothing yet tests `app.js` itself (rendering, the cart flow, offline queuing) or runs in CI automatically. A lightweight next step: a GitHub Action that runs `node tests/run_tests.js` against a throwaway Postgres service container on every push, so a bad schema change can't reach `main` unnoticed.
6. **Battle-test the offline/PWA behavior deliberately.** The service worker and offline-first design are already there (see section 4), but they've been read, not stress-tested: what actually happens on a real phone with real network drops mid-sale, a slow 3G connection at a rural pharmacy, or the app being force-closed mid-transaction. Worth a dedicated round of hands-on testing rather than trusting the code alone.
7. **Cross-device/cross-browser pass.** Verified so far mainly on desktop Chrome via the review's own screenshots. Kenyan pharmacy staff will mostly be on Android phones (Chrome) and possibly older/cheaper devices — worth checking real performance and layout there, plus a quick Safari/iOS check if any pharmacy staff use iPhones.
8. **Load a real-sized dataset and check performance.** Everything's been tested with a handful of drugs/batches. Before a pharmacy with hundreds of SKUs and years of sales history goes live, confirm inventory search, reports, and Excel export all stay fast at that scale.

### C. Compliance & business-readiness
9. **eTIMS (KRA e-invoicing) integration** (item 9) — not urgent today, but a real deadline for VAT-registered pharmacies. The schema already has the fields to support it without a rebuild; the integration work itself hasn't started.
10. **Swahili** (item 6) — the toggle is off and the app is English-only for now (done). Finishing the full translation and bringing the toggle back is still open whenever there's appetite for it.

### D. Code health — lower urgency, pays off as the app grows
11. **Refactor the single-file `app.js`** (item 4, 1,700+ lines and growing) into modules once it gets noticeably bigger or a second developer joins — not broken today, just a maintenance cost that compounds.
12. **A staff-facing "who changed what" audit trail** for corrections/voids/write-offs beyond what's already logged, if pharmacy owners start asking "who did this" — not requested yet, flagging as a natural next ask once there's more than one staff account in real use.

### Suggested next session's focus
Section A is now done from code — the two things left in it (A.3 backups, A.5 leaked-password protection) are one-click Supabase dashboard settings no available tool can flip; do those yourself, they take under two minutes combined. After that, B.6–B.7 (real-device testing) is what actually earns the "reliable like Shule Web" label — a real Android phone with a flaky connection, mid-sale force-closes, and a realistic-sized dataset — and it's genuinely something that needs a physical device or the user's own hands-on testing, not something a code review from here can substitute for.

**Reminder for whoever picks this up:** after any further code change, the delivery workflow is: edit in the Claude sandbox → verify (`node --check`, real headless-browser render, screenshot) → `SendUserFile` → `device_commit_files` into `C:\Users\user\Downloads\Pharma` → tell the user the exact `git add`/`commit`/`push` commands to run themselves (Claude never runs git for them). Netlify auto-deploys on every push to `main` — no separate deploy step needed once pushed. Edge Functions deploy separately, straight to Supabase (not through git/Netlify) — see `mcp__Supabase__deploy_edge_function` or the Supabase CLI (`supabase functions deploy <name>`).
